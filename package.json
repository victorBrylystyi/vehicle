{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildDemo": "npm --prefix demo run build",
    "gitUpdate": "git add . && git commit -m 'test' && git push -u origin main",
    "build": "webpack --mode production && npm run buildDemo && npm run gitUpdate",
    "watch": "webpack --watch",
    "dev": "npm run watch && npm --prefix demo start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.37.1",
    "webpack-cli": "^4.7.0"
  },
  "dependencies": {
    "cannon": "^0.6.2",
    "dat.gui": "^0.7.7",
    "event": "^1.0.0",
    "three": "^0.128.0"
  }
}
