{
  "name": "sandbox",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku": "git subtree push --prefix demo/out heroku master && heroku open",
    "git": "git add . && git commit -m 'car' && git push -u origin main",
    "herokuBuild": "npm run build && npm --prefix demo run build && npm run git && npm run heroku",
    "build": "webpack --mode production",
    "watch": "webpack --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.37.1",
    "webpack-cli": "^4.7.0"
  },
  "dependencies": {
    "cannon": "^0.6.2",
    "dat.gui": "^0.7.7",
    "event": "^1.0.0",
    "three": "^0.128.0"
  }
}
